{
  "name": "storage-api",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "babel src/* -d dist && cp .env dist",
    "start": "npm run build && node ./dist/index.js",
    "startw": "npm run buildw && node ./dist/index.js",
    "buildw": "babel src/* -d dist && copy .env dist"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.21.5",
    "@babel/core": "^7.21.8",
    "@babel/preset-env": "^7.21.5"
  }
}
